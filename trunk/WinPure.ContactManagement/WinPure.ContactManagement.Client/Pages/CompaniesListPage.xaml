<CommonControls:PageControl x:Class="WinPure.ContactManagement.Client.Pages.CompaniesListPage"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:CommonControls="clr-namespace:WinPure.ContactManagement.Client.CommonControls;assembly=WinPure.ContactManagement.Client.CommonControls"
                            xmlns:ViewModels="clr-namespace:WinPure.ContactManagement.Client.ViewModels;assembly=WinPure.ContactManagement.Client.ViewModels"
                            mc:Ignorable="d"
                            d:DesignHeight="430"
                            d:DesignWidth="676"
                            Text="Companies"
                            BorderColor="#FF373737">
	<CommonControls:PageControl.Resources>
		<DataTemplate x:Key="CompanyDataTemplate">
			
				<Grid  Background="{x:Null}" Margin="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" d:DesignWidth="429.5" d:DesignHeight="77.963">
					<Grid.RowDefinitions>
						<RowDefinition  />
						<RowDefinition  />
					</Grid.RowDefinitions>
					<TextBlock HorizontalAlignment="Stretch" Margin="8,8,0,8" TextWrapping="Wrap" Text="{Binding Name}" FontFamily="Century Gothic" FontSize="16" Foreground="White" VerticalAlignment="Center"/>
					<TextBlock HorizontalAlignment="Left" Margin="8,0,0,8" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Country}" Foreground="#34FFFFFF" FontSize="13.333" FontFamily="Century Gothic" VerticalAlignment="Center"/>
				</Grid>
		</DataTemplate>
	</CommonControls:PageControl.Resources>

    <CommonControls:PageControl.Background>
        <LinearGradientBrush EndPoint="0.5,1"
                             StartPoint="0.5,0">
            <GradientStop Color="#FF4881AE"
                          Offset="0" />
            <GradientStop Color="#FF1E4762"
                          Offset="1" />
        </LinearGradientBrush>
    </CommonControls:PageControl.Background>

    <CommonControls:PageControl.DataContext>
        <ViewModels:CompaniesViewModel />
    </CommonControls:PageControl.DataContext>

    <Grid Height="353" Width="658">
    	<ListBox Margin="8,8,125,60" BorderThickness="0" Background="{x:Null}" ItemTemplate="{DynamicResource CompanyDataTemplate}" ItemsSource="{Binding Companies}" HorizontalContentAlignment="Stretch" ItemContainerStyle="{DynamicResource ListBoxItemStyle}"/>
      

    </Grid>
</CommonControls:PageControl>
